$("#add_connection_dialog").dialog({
  buttons: {
    'Save Connection': function() {
      $('#connection_form').submit();      
    },
    Cancel: function() {
		  $(this).dialog('close');
		}
  }
});

$("#add_connection_dialog").html("<%=js_escape_html partial "connections/form", :object => @connection %>");